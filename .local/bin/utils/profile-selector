#!/bin/zsh

# Set ws variable to the output of the 'wofi' command
ws="$(echo 'justNeto \n University' | wofi --show=dmenu)"

# Display a notification with the value of $ws
notify-send $ws

alacritty -e firefox -P $ws
